# UnrealScript-TmLanguage

## Contributing

1. `npm install         # install the dependencies`
2. `npm run compile     # converts the *.YAML-tmLanguage files to ./out/*.tmLanguage.json`
3. `npm run test        # also runs compile`
